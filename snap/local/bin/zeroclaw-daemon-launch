#!/bin/sh
# zeroclaw-daemon-launch — persistent gateway service.

export PATH="$SNAP/bin:$PATH"
mkdir -p "$HOME/workspace"

exec "$SNAP/bin/node" "$SNAP/bin/zeroclaw-wrapper.js" daemon --host 0.0.0.0 --port 3000
